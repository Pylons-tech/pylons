apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "{{ .Release.Name }}.fullname" . }}-test-connection"
  labels:
    {{- include "{{ .Release.Name }}.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "{{ .Release.Name }}.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
