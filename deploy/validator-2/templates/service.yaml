apiVersion: v1
kind: Service
metadata:
  name: {{ include "validator-2.fullname" . }}
  labels:
    {{- include "validator-2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "validator-2.selectorLabels" . | nindent 4 }}
