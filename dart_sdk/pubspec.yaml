name: pylons_sdk
description: Pylons sdk connects client apps to the pylons wallet. It helps the client apps to perform actions on the pylons chain via wallet.
version: 0.1.2
homepage: https://www.pylons.tech/
repository: https://github.com/Pylons-tech/pylons_dart_sdk

environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  dartz: ^0.10.0
  equatable: ^2.0.3

  decimal: ^2.1.0


  #Used it for ipc
  uni_links: ^0.5.1
  uni_links_platform_interface: ^1.0.0


  url_launcher: ^6.1.2
  protobuf: ^2.0.0

  fixnum: ^1.0.0

  bitstream: ">=0.0.4 <2.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter

  mockito: ^5.0.16

  #  code_builder: ^4.0.0
  build_runner: ^2.1.4




flutter:
