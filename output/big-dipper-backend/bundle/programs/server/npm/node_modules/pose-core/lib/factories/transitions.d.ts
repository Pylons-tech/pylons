import { PoseMap, TransitionMapFactory, TransitionMap } from '../types';
declare const generateTransitions: <A, TD>(poses: PoseMap<A, TD>, defaultTransitions: Map<string, TransitionMap<A, TD> | TransitionMapFactory<A, TD>>) => PoseMap<A, TD>;
export default generateTransitions;
