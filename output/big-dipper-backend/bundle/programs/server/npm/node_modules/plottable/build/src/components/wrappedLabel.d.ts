/**
 * Copyright 2014-present Palantir Technologies
 * @license MIT
 */
import { SpaceRequest } from "../core/interfaces";
import { Label } from "./label";
export declare class WrappedLabel extends Label {
    protected _maxLines: number;
    requestedSpace(offeredWidth: number, offeredHeight: number): SpaceRequest;
    /**
     * Get the label max number of wrapped lines.
     */
    maxLines(): number;
    /**
     * Set the label's max number of wrapped lines.
     * @param maxLines
     */
    maxLines(maxLines: number): this;
}
