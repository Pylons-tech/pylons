apiVersion: extensions/v1beta1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: testnet
spec:
  ingress:
  - from:
    - podSelector:
        matchLabels:
          io.kompose.network/testnet: "true"
  podSelector:
    matchLabels:
      io.kompose.network/testnet: "true"
